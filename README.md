大家好，我是黃楷傑，這是我試著做的員工座位系統。  
我本身對java和SQL有一定基礎，然而我對於Spring Boot、Vue等框架是第一次接觸，所以可能只能做出部分框架，且無法執行。  
這兩天的透過網路上的Spring Boot教學，我將該系統設計成由Controller、Service、Repository組成的三層式架構，  
Repository層負責所有資料庫存取、Service層負責商業邏輯處理、Controller層負責接收前端傳過來的Http request，並且去驗證請求參數  

目前完成的部分為資料庫方面的設計(DDL、DML、Stored Procedure)  
Repository層和Service層只做了比較基礎的部分  
